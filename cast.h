#ifndef LAV_CAST
#define LAV_CAST

/* convert a value of type real to one of type string */
obj * rtos(obj * real);

/* convert a value of type string to one of type real */
obj * stor(obj * string);



#endif
